name = "heatsync-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Custom domain routing
routes = [
  { pattern = "heatsync.now", custom_domain = true }
]

# Cron trigger - every 4 minutes to prevent Koyeb deep sleep
[triggers]
crons = ["*/4 * * * *"]
